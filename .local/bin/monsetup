#!/bin/sh

state=1

while true; do
	con=$(xrandr | grep HDMI-1 | cut -d ' ' -f 2)
	if [ $con = "connected" ]; then
		if [ $state -eq 1 ]; then
			xrandr --output eDP-1 --off
			state=0
		fi
	else
		if [ $state -eq 0 ]; then
			state=1
		fi
	fi
	sleep 5s
done
